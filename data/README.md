# Data folder

Put your collected files here:

- `raw_papers.jsonl`
- `raw_edges.csv`

The pipeline will generate:

- `cleaned_papers.jsonl`
- `graph_edges.csv`
- `candidate_pairs.csv`

A tiny sample dataset is provided in `data/sample/` only for testing the code structure.
